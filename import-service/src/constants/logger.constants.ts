export enum LoggerConstants {
  IMPORT_FILE_NAME = 'Import file name -',
  IMPORT_SUCCEEDED = 'Import succeeded',
  PROCESSING_RECORD = 'Processing record',
  LOADING = 'Loading',
  DATA_AFTER_PARSING = 'Finished parsing data',
  MOVE_UPLOADED_FILE = 'Move uploaded file to parsed folder',
  FILE_WAS_MOVED = 'File was moved to parsed folder',
  SUCCESS = 'Success',
  PUBLISH_MESSAGE = 'Publish message to queue: ',
}
